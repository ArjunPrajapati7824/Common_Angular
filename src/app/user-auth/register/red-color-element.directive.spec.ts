import { RedColorElementDirective } from './red-color-element.directive';

describe('RedColorElementDirective', () => {
  it('should create an instance', () => {
    const directive = new RedColorElementDirective();
    expect(directive).toBeTruthy();
  });
});
